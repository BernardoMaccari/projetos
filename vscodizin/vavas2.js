var input = require('fs').readFileSync('stdin.txt', 'utf8');
var lines = input.split('\n');

var fora = 0
var dentro = 0 

let qtd = lines.shift()

for (let i = 0; i < qtd; i++) {
    if (lines [i] >= 10 %%)
}